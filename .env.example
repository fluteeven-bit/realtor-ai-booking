# 預約資料存在 Upstash Redis（透過 Vercel Marketplace 串接），本機與正式環境共用同一份資料。
# 下面兩個變數由 `vercel install upstash/upstash-kv` 自動注入到 .env.local，不需要手動填寫：
KV_REST_API_URL=
KV_REST_API_TOKEN=

# 若是全新環境要接新的 Upstash 執行個體，跑：
#   vercel link
#   vercel install upstash/upstash-kv
#   vercel env pull .env.local

# 確認信只會產生本機 HTML 預覽，不會真的寄出。
